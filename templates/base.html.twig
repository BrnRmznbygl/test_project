<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Nexus{% endblock %}</title>
    <link rel="icon" href="/images/logo.png" type="image/png">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    {% block stylesheets %}{% endblock %}
</head>
<body style="margin: 0; padding: 0;">

    <!-- Logo avec menu déroulant -->
    <div class="dropdown" style="position: absolute; top: 10px; left: 10px; z-index: 100;">
        <a href="#" class="dropdown-toggle" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false">
            <img src="/images/logo.png" alt="Logo Nexus" class="img-fluid" style="max-height: 60px;">
        </a>
        <ul class="dropdown-menu" aria-labelledby="dropdownMenuButton1">
            <li>
                <a class="dropdown-item" href="{{ path('app_home') }}">Accueil</a>
            </li>
            <li>
                <a class="dropdown-item" href="{{ path('app_logout') }}">Connexion/Déconnexion</a>
            </li>
        </ul>
    </div>

    <!-- Contenu principal -->
    {% block body %}{% endblock %}

    <!-- Bootstrap JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
